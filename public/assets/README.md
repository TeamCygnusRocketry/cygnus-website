# Replaceable assets

Place the supplied logo at `branding/cygnus-logo.png`.

The supplied Cygnus R1 image and rocket/CanSat CAD renders are now in use under `rocket/` and `cansat/`.

The supplied AVI is retained as `rocket/rocket-assembly-source.avi`. AVI browser support is unreliable; export it as an H.264 MP4 named `rocket/rocket-deployment.mp4` for dependable in-page playback.

The site intentionally displays designed SVG/CSS fallbacks until those files are available, so missing media never appears broken.
